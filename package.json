{
  "name": "proq",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && pip install -r requirements.txt  && cd ..\frontend && pnpm install && pnpm run build",
    "start": "backend/gunicorn SYSTEMMESEGES.wsgi:application",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
